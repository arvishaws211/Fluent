# @angular/fire 20.x has not yet shipped an Angular 21 peer-dep range.
# Until AngularFire ships a 21-compatible release, all installs in this
# repo must run with legacy peer-dep resolution. Pinning this here keeps
# local installs, CI, and Husky's `prepare` step in lockstep.
legacy-peer-deps=true
